<!-- app/src/main/res/layout/fragment_comparador.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Spinner para elegir autoparte -->
    <Spinner
        android:id="@+id/spinnerAutopartes"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <!-- RecyclerView con las cotizaciones -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvComparador"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:layout_weight="1"/>

    <!-- (Opcional) un FAB para agregar cotización manual -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAgregar"
        android:src="@drawable/ic_mas"
        android:contentDescription="Agregar cotización"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|bottom"
        android:layout_margin="16dp"/>
</LinearLayout>
