<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Gráfica de LineChart para gastos -->
    <com.github.mikephil.charting.charts.LineChart
        android:id="@+id/lineChartGastos"
        android:layout_width="match_parent"
        android:layout_height="200dp"/>

    <!-- Gráfica de BarChart para facturas -->
    <com.github.mikephil.charting.charts.BarChart
        android:id="@+id/barChartFacturas"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_marginTop="16dp"/>

    <!-- Texto con total del mes actual -->
    <TextView
        android:id="@+id/tvTotalMes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Total gastado este mes: $0.00"
        android:textSize="16sp"
        android:layout_marginTop="16dp"/>
</LinearLayout>
