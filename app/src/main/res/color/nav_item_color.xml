<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ítem seleccionado → rojo -->
    <item android:state_checked="true" android:color="@color/redCustom"/>
    <!-- ítem no seleccionado → colorOnSurface (por defecto legible) -->
    <item android:state_checked="false" android:color="?attr/colorOnSurface"/>
</selector>
